# Backend Recomendador de autos

## Ejecutar

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
uvicorn app.main:app --reload
```

API local: `http://127.0.0.1:8000/docs`
